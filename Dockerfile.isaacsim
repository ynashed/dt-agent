FROM nvcr.io/nvidia/isaac-sim:5.1.0

# Run as root so docker-compose's volume-mounted code is readable and so
# the entrypoint can launch python.sh (matches OMNI_KIT_ALLOW_ROOT=1 in
# the compose file).
USER root

WORKDIR /workspace/dt-agent
